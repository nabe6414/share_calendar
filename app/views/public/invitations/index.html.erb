<% @invitations.each do |invitation| %>
  <%= invitation.user.name %><%= link_to "取消す", group_invitation_path(invitation.group_id, invitation.id), method: :delete, class: "btn btn-danger" %>
<% end %>